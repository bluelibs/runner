/**
 * Canonical shutdown-abort reason used when Runner escalates from graceful
 * drain into forced task interruption during disposal.
 */
export const runtimeShutdownAbortReason =
  "Runtime shutdown drain budget expired";
