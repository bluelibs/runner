{
  "name": "@runner-examples/durable-workflows",
  "version": "0.1.0",
  "private": true,
  "description": "Runner durable workflows example — order processing & user onboarding",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node dist/index.js",
    "check": "npm run start",
    "test": "npm run build && node --test dist/durable-workflows.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@bluelibs/runner": "file:../.."
  }
}
