{
  "name": "test-with-module",
  "version": "1.0.0",
  "private": true,
  "description": "Runner example with local IDs in a Node module package.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p .",
    "start": "npm run build && node dist/index.js",
    "check": "npm run start"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@bluelibs/runner": "file:../.."
  }
}
